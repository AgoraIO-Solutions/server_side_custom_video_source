## to install, please run:

$ sudo ./install.sh

This will backup any current installation into the current directory

## recover ngnix using a backup

If you would like to recover a previously backed up version, you can run:

$ ./recover.sh /path/to/backup/dir
